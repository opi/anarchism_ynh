location {{ ynh_app_settings.path }} {
  alias /usr/share/doc/anarchism/html/ ;

  # Force https
  if ($scheme = http) {
    rewrite ^ https://$server_name$request_uri? permanent;
  }

  index index.html;

  # Include SSOWAT user panel.
  include conf.d/yunohost_panel.conf.inc;
}
